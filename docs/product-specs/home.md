# 홈

## 목적

사용자가 `daily-listen-up`이 어떤 미니앱인지 빠르게 이해하고, 오늘의 영어 듣기 문제를 시작할 수 있게 한다.

## 진입 조건

- 사용자가 미니앱에 진입한다.

## 사용자에게 보여줄 내용

- 미니앱에 대한 간략한 설명
- 하루에 짧은 영어 듣기 문제 1개를 푸는 서비스라는 점
- 정답 시 토스 포인트 지급이 된다는 점
- 오늘의 문제를 시작할 수 있는 하단 주요 액션

## 문구 초안

- 제목: `오늘의 영어 듣고 포인트 받기`
- 설명: `짧은 영어 음성을 듣고 문제를 맞히면 토스 포인트 보상에 도전할 수 있어요.`
- 보조 설명: `하루에 한 문제만 제공돼요.`
- 주요 버튼(CTA): `시작하기`

## 주요 액션

- `시작하기`
  - 사용자가 누르면 토스 로그인 흐름으로 진입한다.
  - 이미 토스 로그인을 완료한 사용자는 `appLogin()` 호출 시 별도 로그인 화면 없이 인가 코드를 받을 수 있다.
  - 이 경우에도 서버의 `POST /api/login/toss`를 호출해 당일 앱 세션을 새로 확보한다.
  - 로그인과 앱 세션 확보 이후 `GET /api/check-today-quiz`를 호출한다.
  - 처리 과정:
    1. 사용자가 `시작하기`를 클릭한다.
    2. 클라이언트가 `appLogin()`을 호출한다.
    3. 이미 로그인/동의된 사용자면 토스가 바로 `authorizationCode`, `referrer`를 반환한다.
    4. 아직 로그인/동의하지 않은 사용자면 토스 로그인과 약관 동의 화면이 노출되고, 동의 완료 후 `authorizationCode`, `referrer`를 반환한다.
    5. 클라이언트가 `POST /api/login/toss`를 호출한다.
    6. 서버가 새 앱 세션 토큰을 발급한다.
    7. 클라이언트가 `GET /api/check-today-quiz`를 호출한다.
    8. 오늘 문제가 있으면 `GET /api/reward-status`를 호출한다.
    9. 진행 상태와 지급 상태에 따라 문제 화면으로 이동하거나 홈에 토스트를 표시한다.
  - 오늘 영어 듣기 문제가 없으면 홈에 머무르고 토스트로 `오늘의 문제가 아직 준비되지 않았어요.`를 보여준다.
  - 오늘 영어 듣기 문제가 있으면 `GET /api/reward-status`를 호출한다.
  - `GET /api/reward-status` 응답의 `progressStatus`는 오늘 문제 진행 상태이고, `rewardStatus`는 포인트 지급 상태다.
  - `progressStatus`가 `not_started`, `wrong`, `retry_unlocked`이면 오늘의 문제 풀이 화면으로 이동할 수 있다.
  - `progressStatus`가 `completed`이고 `rewardStatus`가 `failed`이면 홈에 머무르고 토스트로 `포인트 지급 확인이 필요해요`를 보여준다.
  - `progressStatus`가 `completed`이고 `rewardStatus`가 `failed`가 아니면 홈에 머무르고 토스트로 `오늘 문제풀이를 완료했습니다`를 보여준다.
  - 기본 상태에서는 활성화한다.
  - 요청 처리 중에는 중복 클릭을 막기 위해 비활성화한다.

## 로그인 실패 또는 취소

- 사용자는 홈에 머문다.
- 토스트 메시지 보여주고 다시 `시작하기`를 누를 수 있게 한다.
- 토스트 메시지 초안: `로그인을 완료하지 못했어요. 다시 시작해 주세요.`

## 지급 상태 확인 실패

- 사용자는 홈에 머문다.
- 토스트 메시지 보여주고 다시 `시작하기`를 누를 수 있게 한다.
- 토스트 메시지 초안: `상태를 확인하지 못했어요. 다시 시도해 주세요.`

## 오늘 영어 듣기 문제 확인 실패

- 사용자는 홈에 머문다.
- 토스트 메시지 보여주고 다시 `시작하기`를 누를 수 있게 한다.
- 토스트 메시지 초안: `상태를 확인하지 못했어요. 다시 시도해 주세요.`

## 오늘 영어 듣기 문제 없음 상태

- 토스트 메시지로 안내한다
- `시작하기` 클릭 후 서버에서 오늘 영어 듣기 문제가 없다고 확인되면 준비 중 상태를 보여준다.
- 토스트 메시지: `오늘의 문제가 아직 준비되지 않았어요.`

## 상세 스펙에서 나중에 정할 것

- 홈 화면의 최종 UI 배치
- TDS 컴포넌트 선택
