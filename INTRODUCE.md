# 📘 daily-listen-up 기획 문서

---

## 1. 🎯 목적 (Goal)

짧은 실생활 영어 오디오를 듣고 퀴즈로 확인하는 방식으로  
**부담 없이 반복 가능한 영어 듣기 학습 경험 제공**

정답을 맞춘 사용자는 **토스 포인트(프로모션)** 를 받을 수 있음

---

## 2. 👤 타겟 사용자 (Target User)

- 영어 듣기 실력을 키우고 싶은 초급 ~ 중급 사용자
- 긴 강의보다 짧은 학습을 선호하는 사용자
- 출퇴근 / 쉬는 시간에 1~2분 학습하는 사용자
- 토스 앱 안에서 가볍게 학습하고 보상까지 받고 싶은 사용자

---

## 3. 📱 서비스 형태

- `Apps in Toss`의 **비게임 카테고리 WebView 미니앱**
- 프론트엔드는 **React 기반**으로 개발
- 앱 이용을 위해 **토스 로그인 필수**

---

## 4. 🔁 핵심 유저 플로우 (User Flow)

앱 진입  
→ 토스 로그인 여부 확인  
→ 미로그인 시 토스 로그인 진행  
→ 오늘의 문제 제공  
→ 오디오 재생  
→ 객관식 문제 풀이  
→ 정답이면 토스 포인트 지급  
→ 오늘 학습 완료 화면 노출  
→ 오답이면 보상형 광고 시청 유도  
→ 광고 보상 완료 시 같은 문제 재도전 가능

---

## 5. ✅ 핵심 정책

### 하루 1문제

- 사용자는 하루에 **오늘의 문제 1개만** 풀 수 있음
- 날짜 기준은 **매일 00:00 KST**
- 이미 당일 문제를 완료한 사용자는 다시 들어와도 완료 화면만 노출

### 재도전 정책

- 오답 시 바로 재도전할 수 없음
- **보상형 광고를 끝까지 시청해 보상 이벤트를 받은 경우에만** 재도전 가능
- 같은 문제에 대한 재도전 횟수는 **무제한**
- 광고를 닫기만 한 경우에는 재도전 기회를 주지 않음

### 보상 정책

- 정답을 맞춘 경우 **토스 포인트(프로모션)** 지급
- 포인트는 클라이언트에서 직접 처리하지 않고 **서버를 통해 지급**
- 서버에서 **1인 1일 1회 지급**을 보장해야 함
- 동일 날짜에 중복 요청이 들어와도 멱등하게 처리해야 함

---

## 6. 🔐 로그인 / 사용자 식별

- 앱 진입 시 **토스 로그인 필수**
- 클라이언트는 `appLogin()`으로 인가 코드만 받음
- 서버가 인가 코드를 토큰으로 교환하고 사용자 정보를 조회해 `userKey`를 확보함
- 이후 진행 상태와 보상 지급은 **`userKey` 기준**으로 관리

---

## 7. 🧩 데이터 구조 초안

### 문제 데이터

`quizzes` 컬렉션에 저장

- `quizDate`
- `audioUrl`
- `questionText`
- `choices`
- `correctChoiceIndex`
- `script`
- `promotionAmount`

### 사용자 진행 상태 데이터

`userKey + quizDate` 기준으로 저장

- `quizDate`
- `status`
- `attemptCount`
- `hasEarnedRetry`
- `isPromotionGranted`
- `promotionGrantKey`
- `promotionStatus`
- `lastSubmittedAt`

---

## 8. 🗂️ 콘텐츠 운영 방식

- 초기 버전은 **5일치 문제를 수동으로 제작**
- 문제 데이터는 **Firebase**에 저장
- 오늘 날짜에 맞는 문제를 앱에서 조회
- 오늘 문제 데이터가 없으면 `오늘의 문제 준비 중` 상태를 보여줌

---

## 9. 🧱 시스템 역할 분리

### 이 레포에서 다룰 범위

- React 기반 앱 UI
- Firebase에 저장된 문제 조회
- Firebase 기반 사용자 진행 상태 관리
- 토스 로그인 클라이언트 연동
- 서버 API를 호출하는 인터페이스 정의

### 서버에서 다룰 범위

- 인가 코드 → access token / refresh token 교환
- 사용자 정보 조회 후 `userKey` 확보
- 정답 검증
- 일일 1회 지급 판정
- 프로모션 key 발급
- 프로모션 실행
- 프로모션 결과 조회
- 지급 결과 및 상태 멱등 저장

### 이번 범위에서 제외

- 매일 AI가 문제를 생성하는 자동 배치 서버
- 실제 운영용 관리자 화면

---

## 10. 🧪 MVP 완료 기준

- 사용자는 토스 로그인 후 오늘의 문제를 볼 수 있어야 함
- 문제 오디오를 듣고 객관식 답안을 제출할 수 있어야 함
- 정답/오답 결과에 따라 분기된 화면을 볼 수 있어야 함
- 오답 시 광고 보상 완료 후 다시 제출할 수 있어야 함
- 정답 시 토스 포인트 지급 요청이 정상적으로 시작되어야 함
- 이미 보상을 받은 사용자는 같은 날짜에 중복 지급되지 않아야 함

---

## 11. 🔜 다음 단계

- 초기 5일치 문제와 오디오 파일 준비
- Firebase 컬렉션 구조 확정
- 토스 로그인 / 프로모션 서버 API 명세 확정
- React 앱 기본 화면 및 상태 흐름 구현
